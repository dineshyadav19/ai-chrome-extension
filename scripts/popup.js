document.addEventListener('DOMContentLoaded', function () {
  const button = document.getElementById('bottomButton');
  button.addEventListener('click', function () {
    // Your button's click functionality here
    alert('Button clicked!');
  });
});
